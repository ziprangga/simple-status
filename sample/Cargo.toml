[package]
name = "sample"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
simple-status = { path = ".." }
iced = { version = "0.14", features = [
    "tokio",
    "wgpu",
] }
tokio = "1"
